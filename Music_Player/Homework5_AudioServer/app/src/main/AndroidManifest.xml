<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.viswas.KeyCommon">

    <application
        android:allowBackup="false"
         >

        <service
            android:label="@string/app_name"
            android:name="com.example.viswas.audioserver.AudioServer"
            android:stopWithTask="true"
            android:exported="true"
            >


            <intent-filter>
                <action android:name="com.example.viswas.PlayMusic" />
            </intent-filter>
        </service>
    </application>

</manifest>